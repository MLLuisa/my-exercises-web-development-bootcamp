"use strcit";

const gatito = {
    nombre: "Misifu",
    edad: 19,
    imagen: "http://placekitten.com/200/300"
};

/*query selector name-iamge-desc-button-list;
add event listener click abd handleclicksave;
crear function handleclicksave (event.prevent.default) --> tomar los valores de los inputs (nameInput, imageInput,descInput);
generar el html para que aparezca en la pagina (const html) (copiar linea 19-25 pero solo con los que necesitamos);
despues listElement.innerhtml += html;

*/